# RealSense 自然语言目标定位：代码结构说明

本文对应项目版本 `0.4.0`，说明当前代码的目标、数据流、模块边界、关键算法、线程模型、配置入口、测试布局和后续扩展点。

## 1. 项目边界

本项目当前完成的是：

```text
RealSense D435 采集当前视野
→ YOLO 检测常见物体
→ 跨帧维护物体 ID
→ 从对齐深度估计物体三维位置
→ 用自然语言选择一个候选目标
→ 输出目标相对相机的距离和方向角
```

当前不负责地图、SLAM、全局路径规划、动态避障、机器人坐标变换或底盘控制。输出角度是目标相对于当前相机光轴的观测方向，不是机器人最终应执行的导航路径。

## 2. 顶层目录

```text
realsense/
├── main.py                         程序入口
├── README.md                       Ubuntu 安装、运行和参数说明
├── 代码结构说明.md                  本文
├── 对话记录.md                      原始需求与过往实施对话
├── requirements.txt                pip 运行/测试依赖
├── environment.yml                 Conda Python 3.11 基础环境
├── pyproject.toml                  项目元数据与 pytest 配置
├── local_config.example.py         可复制的本地配置模板
├── local_config.py                 用户本地密钥/模型配置，不应提交
├── .env.example                    环境变量示例
├── realsense_nav/                  业务代码包
│   ├── app.py                      CLI、主循环、线程和模块编排
│   ├── camera.py                   D435 枚举、采集、对齐、滤波、旋转
│   ├── detector.py                 YOLO26 推理和 BoT-SORT 跟踪
│   ├── geometry.py                 稳健深度、反投影、距离和角度
│   ├── memory.py                   目标记忆、平滑、重捕获和候选确认
│   ├── models.py                   模块间数据结构
│   ├── selector.py                 本地规则与 DeepSeek 自然语言选择
│   ├── settings.py                 配置读取优先级
│   ├── tracking.py                 轻量 IoU 后备跟踪器
│   ├── visualizer.py               彩色/深度实时视图、图例和目标标注
│   └── __init__.py                 包版本
├── scripts/
│   ├── setup_ubuntu.sh             Ubuntu + CUDA + 阿里云镜像安装
│   ├── run_tests.sh                隔离 ROS 全局插件的测试入口
│   └── setup_env.ps1               保留的 Windows 安装入口
├── tests/                           不依赖真实相机的单元测试
│   ├── test_app.py                 CLI 默认值
│   ├── test_camera.py              旋转后相机内参
│   ├── test_geometry.py            深度和方向计算
│   ├── test_memory.py              记忆、平滑、过期和重绑定
│   ├── test_selector.py            中英文选择与 API 输出验证
│   ├── test_settings.py            配置优先级
│   ├── test_tracking.py            IoU 后备跟踪
│   └── test_visualizer.py          深度着色、图例和视图组合
├── yolo26m.pt                      默认高精度预训练权重
└── diagnostics/                    实机诊断抓拍，已在 .gitignore 忽略
```

`yolo26n.pt` 是旧版最小权重，可以保留作低性能设备降级；当前默认权重由配置和 CLI 指向 `yolo26m.pt`。

## 3. 主调用链

`main.py` 只导入并调用 `realsense_nav.app.main()`。真正的运行逻辑位于 `app.py`：

```text
main.py
└── app.main(argv)
    ├── build_parser()                         解析 CLI/本地配置
    ├── check_device(args)                     仅设备诊断模式
    └── run(args)                              完整运行模式
        ├── build_selector(...)
        ├── YoloDetector(...)
        ├── RealSenseCamera(...)
        ├── TargetMemory(...)
        └── while frame:
            ├── camera.get_frame()
            ├── detector.detect(color)
            ├── localize_detection(...) × N
            ├── memory.update(...)
            ├── selector.select(command, snapshot)  异步
            ├── print selection/direction
            └── draw_live_view(...)            非 headless 彩色/深度视图
```

主循环只在一个线程中修改相机、检测器和目标记忆，避免这些有状态对象发生并发竞争。终端输入使用守护线程；DeepSeek 请求使用单工作线程的 `ThreadPoolExecutor`，不会阻塞相机采集和窗口刷新。

## 4. 核心数据结构

数据结构集中在 `realsense_nav/models.py`。

### `Detection`

表示一帧中的视觉跟踪结果：

```text
track_id       BoT-SORT 或后备跟踪器生成的稳定编号
class_name     COCO 英文类别
confidence     当前/平滑后的置信度
bbox           (x1, y1, x2, y2)，像素坐标
```

### `LocalizedObject`

在 `Detection` 上增加空间和记忆信息：

```text
x_m, y_m, z_m          相机坐标（米）
distance_m             水平距离 hypot(X, Z)
signed_angle_deg       相对相机正前方偏角，左负右正
direction_deg          0°左 / 90°前 / 180°右
depth_sample_count     本次深度估计采用的像素数
depth_mad_m            深度中位绝对偏差，越小越稳定
is_visible             当前是否仍有视觉检测框
last_seen_age_s        距最后视觉观测的秒数
depth_age_s            距最后有效深度的秒数
observation_count      累计有效观测次数
```

`as_selector_dict()` 只导出自然语言选择需要的结构化字段。大模型看不到原始图像或深度数组，也不能直接生成坐标。

### `SelectionResult`

自然语言选择器的统一返回类型：

```text
status          matched / ambiguous / not_found
target_id       唯一匹配的目标 ID
candidate_ids   有歧义时的候选 ID
reason          面向用户的原因
source          local / deepseek / local-fallback
```

## 5. 相机层：`camera.py`

`RealSenseCamera` 封装 `pyrealsense2`

启动顺序：

1. 可按 `--serial` 绑定指定相机；
2. 开启 `848×480` 深度流和 `640×480` 彩色流；
3. 使用 `rs.align(rs.stream.color)` 将深度对齐到彩色像素；
4. 预热若干帧，让自动曝光与深度时间滤波稳定；
5. 读取深度比例，将 `z16` 转成米。

默认深度处理链：

```text
aligned depth
→ spatial_filter
→ temporal_filter
→ hole_filling_filter
├→ float32 meters                         定位和计算
└→ rs.colorizer + histogram equalization  官方风格显示
```

`CameraFrame` 包含 BGR 图、米制深度图、SDK 官方深度彩图、对齐后的内参和时间戳。`headless` 模式不创建 SDK 着色器，避免为不可见画面消耗处理时间。

`--rotate 90/180/270` 会同步旋转彩色图、深度图和内参。旋转后使用针孔反投影计算显示坐标系中的三维方向；不能只旋转彩色图，否则检测框与深度会错位。

`list_devices()` 额外报告序列号、固件、产品 ID 和 USB 协议。D435 应连接 USB 3.x；USB 2.0 会限制可用流配置和稳定性。

## 6. 检测与跟踪：`detector.py`

`YoloDetector` 加载 Ultralytics 权重并调用 `model.track()`：

```text
输入 BGR ndarray
→ YOLO26m 640 像素推理
→ conf >= 0.15 的框进入 BoT-SORT
→ persist=True 保留卡尔曼状态
→ 输出带 track_id 的 Detection
```

使用 BoT-SORT 的原因：

- 有线性卡尔曼运动模型；
- 有轨迹缓冲，能跨越短暂遮挡或低置信帧；
- 默认包含相机运动补偿，适合机器人或手持相机移动；
- 比旧版仅比较相邻框 IoU 更不容易跳号。

检测最低阈值与候选阈值被有意分离：

```text
--confidence 0.15
    允许低置信框参与轨迹延续，减少掉轨。

--candidate-confidence 0.30
    只有达到该平滑置信度的确认轨迹才交给自然语言选择。
```

若 Ultralytics 没返回 ID，`tracking.py` 的 `IouTracker` 作为后备。它仍只按同类别 IoU 贪心匹配，不应作为正常生产跟踪器。

## 7. 深度与几何：`geometry.py`

### 前景深度簇

普通的“框内所有像素取中位数”会在椅子、桌子、栏杆等镂空目标上选到后方墙面。本版算法：

1. 截取检测框中央区域；
2. 去掉 NaN、零值和范围外深度；
3. 对深度排序；
4. 按自适应深度断层分簇；
5. 在像素数量可信的簇中选择最近簇；
6. 对该簇再次使用中位数与 MAD 去除飞点；
7. MAD 超过 `--max-depth-mad` 时拒绝本次深度。

`DepthSample` 保存代表像素、深度、像素数、MAD 和有效像素比例。

### 反投影与方向

正常未旋转画面优先调用 RealSense SDK：

```python
rs.rs2_deproject_pixel_to_point(intrinsics, [u, v], depth_m)
```

得到相机坐标 `(X, Y, Z)` 后：

```text
水平距离 = hypot(X, Z)
偏转角 = degrees(atan2(X, Z))
方向角 = clamp(90° + 偏转角, 0°, 180°)
```

## 8. 目标记忆：`memory.py`

`TargetMemory` 是解决“识别一次后很快失焦”的核心状态机，位于检测器和选择器之间。

### 轨迹状态

```text
未确认
  └── observation_count >= 2 且 confidence >= 0.30
      └── 可作为自然语言候选
          ├── 当前可见：实时更新框与平滑三维位置
          ├── 视觉可见但深度失败：更新框，沿用最近有效深度
          ├── 暂时不可见：保留最后框和三维位置
          └── 超过 TTL：删除
```

普通目标 TTL 默认 4 秒；选中目标 TTL 默认 15 秒。所有 TTL 使用 `time.monotonic()`，不会被系统时间调整影响。

### 平滑

同一 ID 的三维坐标和置信度使用 EMA：

```text
smooth = alpha × current + (1 - alpha) × previous
```

默认 `alpha=0.35`。如果两次三维位置跳变超过距离相关阈值，本帧会直接采用新值，避免把两个明显不同位置错误平均。

### 重捕获

已选目标消失后，若 BoT-SORT 重新出现同类但给出新 ID，记忆层综合：

- 类别一致；
- 框 IoU；
- 三维位置差；
- 方向角差；

判断是否是原目标。满足门限时不只改写当前帧，而是登记持久的 `新 ID → 原 ID`
别名，保持用户锁定语义不变。别名在 memory 更新前同时作用于三维观测和原始
detection，因此深度暂时失效时仍能用新跟踪框更新原目标，而不会把原目标误判为
不可见。

如果 BoT-SORT 在切换期间同帧输出旧、新两个 ID，`TargetMemory` 会执行以下处理：

1. 只对强重叠且三维位置接近的同类框建立别名，避免合并附近的两个真实物体；
2. 删除新 ID 已经产生的重复 memory 条目，以原选中 ID 为权威条目；
3. 对归一后相同 ID 的观测和 detection 去重，置信度更高的框用于当前画面；
4. `app.py` 将归一后的 detection 交给彩色图和深度图绘制，选中框、距离和 ID 保持一致；
5. 原目标超过 TTL 被删除时同步删除对应别名，防止过期 ID 被错误复活。

因此重绑定后即使跟踪器连续输出新号码，候选列表、memory、选中状态和画面标签仍只
使用原号码。

## 9. 自然语言选择：`selector.py`

所有选择器实现同一个 `TargetSelector` 协议。

### `LocalRuleSelector`

完全离线，支持：

- COCO 80 类常用中英文别名；
- `#3`、`编号3`、`3号`；
- 左、右、中央；
- 最近、最远；
- 从左到右第一/第二等序号；
- “人旁边的椅子”这类以最后一个物体名为目标的表达。

当前可见同类存在时，普通类别命令优先当前可见对象，避免旧记忆目标制造歧义；明确按编号仍可选记忆目标。

### `DeepSeekSelector`

按 DeepSeek 的 OpenAI 兼容示例构造客户端，但所有请求只发送到 `DEEPSEEK_BASE_URL`：

```python
client = DeepSeekClient(api_key=deepseek_key, base_url=deepseek_base_url)
response = client.responses.create(
    model=deepseek_model,
    instructions=system_prompt,
    input=candidate_json,
)
```

响应只读取 `response.output_text`，再使用 Pydantic 严格解析 `_LLMSelection`。空文本或非法 JSON 最多补试一次，并验证模型返回的目标 ID 确实存在。默认模型为 `deepseek-v4-flash`。项目没有 OpenAI provider、OpenAI模型、OpenAI密钥或相应 CLI 分支。

客户端固定通过 `DefaultHttpxClient(trust_env=False)` 直连 DeepSeek，始终忽略系统代理环境变量，防止格式错误的 SOCKS 代理在请求前导致初始化失败。项目不提供开启环境代理的配置入口。

### `FallbackSelector`

`auto` 模式中，API、网络或输出校验异常不会终止相机主循环，而是调用本地规则并在 `reason` 中报告回退原因。

配置读取优先级由 `settings.py` 统一定义：

```text
环境变量 > local_config.py > 代码默认值
```

## 10. 可视化：`visualizer.py`

当前可见目标使用实线框，颜色由目标 ID 稳定生成；选中目标使用绿色粗框。标签显示：

```text
#ID class confidence distance direction [depth-age]
```

记忆中但当前不可见的目标使用虚线框和 `MEMORY Ns` 标签；选中的记忆框显示为黄色。`depth-age` 表示视觉仍在但当前深度无效，距离来自最近有效深度。

默认 `official` 模式直接显示 `camera.py` 中 librealsense `rs.colorizer` 的输出。官方着色器启用直方图均衡，把当前帧中密集的距离区间展开，视觉层次接近 RealSense Viewer。`AUTO` 图例从当前帧深度和官方彩图共同采样，颜色与 5 个米制分位刻度保持对应。

`colorize_depth()` 是可按 `H` 切换的 `metric` 模式：使用固定 `--min-depth`～`--max-depth` 范围转换成 Turbo 伪彩色。固定量程更适合跨帧按颜色做定量比较，但大范围量程会压缩室内近距离细节。`colorize_depth_histogram()` 仅作为没有 SDK 彩图输入时的后备实现。

两种模式都会把 `0`、NaN、无穷值和范围外数据显示为黑色。

`draw_depth_scene()` 在对齐深度图上增加：

- 当前检测框和目标的 `Z` 深度；
- 画面中央十字和实时中心像素深度；
- 5 档米制彩色图例及 `near`/`far` 标记，官方模式动态、固定模式等距；
- 黑色 `invalid` 图例，明确区分无效数据和有效远距离。

`draw_live_view()` 支持三种模式：

```text
split    彩色检测画面和对齐深度图并排，默认
color    仅彩色检测画面
depth    仅深度画面
```

运行中按 `D` 由 `next_view_mode()` 循环切换布局，按 `H` 由 `next_depth_color_mode()` 切换官方/固定着色；两者都不影响相机采集、YOLO 跟踪或目标记忆。

## 11. CLI 与运行模式：`app.py`

### 设备诊断

```bash
python main.py --check-device --diagnostic-dir diagnostics
```

此模式不加载 YOLO，只验证设备枚举、流配置、深度覆盖和抓图。输出：

```text
color.png
depth_colormap.png
preview.png
```

### 交互模式

```bash
python main.py
```

窗口默认显示 `split` 视图和 `official` 深度着色；可以使用 `--view-mode color|depth|split` 与 `--depth-color-mode official|metric` 指定初始模式。深度图与彩色图逐像素对齐，因此两侧的检测框坐标一致。

终端输入线程将命令放入 `queue.Queue`；主线程取当前确认候选的不可变快照，再异步调用选择器。即使 API 返回期间场景变化，结果也只针对提交时的候选快照解释。

### 脚本模式

```bash
python main.py --headless --max-frames 100 --selector local --command "去最近的物体"
```

`--command` 会等待目标完成确认，最多等待 `--command-wait-frames`，不再在第一帧就对空候选执行。

## 12. 安装与依赖

`environment.yml` 只创建 Python 3.11 基础环境。`scripts/setup_ubuntu.sh` 再完成：

1. 创建或更新 `realsense` Conda 环境；
2. 从阿里云 CUDA 12.1 wheel 目录安装 GPU PyTorch/torchvision；
3. 从阿里云 PyPI 安装 `requirements.txt`；
4. 打印 CUDA 可用状态和 GPU 名称。

主要依赖职责：

```text
pyrealsense2       D435 SDK
ultralytics        YOLO26 和 BoT-SORT
torch/torchvision  GPU 推理
lap                跟踪器线性分配
opencv-python      图像格式、窗口、绘制和诊断图
numpy              深度数组与稳健统计
openai 3.x         仅作为 DeepSeek Responses API 的兼容客户端
pydantic           大模型输出结构校验
pytest             单元测试
```

## 13. 测试策略

运行：

```bash
conda activate realsense
bash scripts/run_tests.sh
```

脚本设置 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`，防止已经 source 的 ROS Humble 环境把 `/opt/ros` 下 Python 3.10 pytest 插件自动装入 Python 3.11 测试进程。

单元测试不要求 D435、GPU 或网络。实机验证需要另行运行：

```bash
python main.py --check-device --diagnostic-dir diagnostics
python main.py --headless --max-frames 90 --selector local --command "去最近的物体"
```

## 14. 本次三轮改进记录

### 第一轮：Ubuntu、硬件和模型基线

实施：

- 实际创建 Python 3.11 `realsense` 环境；
- 使用 RTX 4060 和 CUDA 12.1 PyTorch；
- 增加 Ubuntu/阿里云安装脚本；
- 增强 D435 USB/深度/诊断图检查；
- 默认权重升级到 `yolo26m.pt`。

重新审视结论：模型更准仍不能解决相邻帧掉框；旧 IoU 跟踪和瞬时帧候选是主要失焦来源。

### 第二轮：跨帧跟踪与语义目标记忆

实施：

- 改用持久 BoT-SORT；
- 增加候选确认、普通/选中目标 TTL；
- 增加三维 EMA；
- 增加选中目标重捕获和 ID 重绑定；
- 重绑定采用持续 ID 别名，删除旧/新 ID 同帧造成的重复 memory；
- 增加记忆状态可视化。

重新审视结论：视觉检测与深度有效性被错误绑定；某一帧深度空洞仍会让目标从定位列表消失。

### 第三轮：定位质量与实际操作可靠性

实施：

- 分离“视觉可见”和“深度最近更新”；
- 使用前景深度簇、中位数、MAD 和质量门限；
- 分离跟踪低阈值与语言候选高阈值；
- 脚本命令等待候选稳定；
- 支持同步旋转彩色/深度/内参；
- 隔离 ROS 测试插件，补充单元测试和工程文档。

重新审视结论：当前算法链已适合做独立视觉原型；下一阶段最有价值的提升不是继续堆通用模型，而是为机器人实际环境采集目标类别数据、标注并微调，同时确定相机安装外参和底盘接口。

### 后续增量：实时深度可视化

- 默认合成彩色/深度并排画面；
- 支持启动参数和 `D` 热键切换视图；
- 默认接入 librealsense 官方直方图均衡彩图，提升室内距离层次；
- 保留固定米制量程并支持 `H` 热键即时对比；
- 增加动态/固定多刻度图例、中心点读数及无效深度说明；
- 设备诊断保存的 `depth_colormap.png` 和 `preview.png` 同样带图例。

## 15. 后续扩展建议

### 自定义类别

COCO 模型不会可靠识别充电桩、实验设备、专用工具箱等对象。应采集真实相机视角数据，划分训练/验证集并微调检测或分割模型。若目标深度容易混入背景，实例分割掩膜比矩形框更合适。

### 接入机器人

机器人型号确定后，不应改写检测/选择模块，只需在稳定的 `LocalizedObject` 后增加：

```text
相机坐标
→ 相机到 base_link 外参
→ 机器人定位坐标/地图坐标
→ 目标前安全停靠点
→ Nav2 或厂商底盘接口
```

### 长时间记忆

当前记忆只在相机当前坐标系中有效。如果机器人自身移动，超过短时间后旧三维坐标会失真。要做跨视野、跨位置长时记忆，必须引入相机运动估计或机器人位姿，将目标转换到稳定世界坐标系；不能简单把 TTL 无限增大。

### 性能部署

当前 RTX 4060 已能实时运行 `yolo26m`。若迁移到边缘设备，可按顺序评估：

1. `yolo26s.pt`；
2. FP16；
3. ONNX Runtime CUDA；
4. TensorRT；
5. 降低 `imgsz`，而不是先提高检测阈值。
